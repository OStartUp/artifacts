---
# Source: test-chart/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: test-test-chart
  labels:

    helm.sh/chart: test-chart-0.1.0
    app.kubernetes.io/name: test-chart
    app.kubernetes.io/instance: test
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
---
# Source: test-chart/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: test
spec:
  selector:
    app: test-deploy
  ports:
    - port: 80
      targetPort: 5000
---
# Source: test-chart/templates/deployment.yaml
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: test
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: test-deploy
    spec:
      containers:
        - name: test-server
          image: marcecaro/test@sha256:32cd4bc6ce31e01411b8d669e8915cc9b96bff04fe3a03061b3a7ca0da9b6a1b
          imagePullPolicy: Always
          # ports:
          # - containerPort: 5000
---
# Source: test-chart/templates/ingress.yaml
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: test
  # annotations:
  #   nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /runtests
        backend:
          serviceName: test
          servicePort: 80
